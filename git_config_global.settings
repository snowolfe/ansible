git config --global user.name "Bruce Wolfe"
git config --global user.email "valid@example.com"
git config --global push.default simple
git config --global credential.https://github.com.username snowolfe
git config --global credential.helper cache --timeout=7200

